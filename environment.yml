# Environment for cjm-transcription-plugin-whisper
# Provides OpenAI Whisper with PyTorch CUDA support
name: cjm-transcription-plugin-whisper
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  # Python version
  - python=3.12

  # System dependencies (conda-managed for proper linking)
  - ffmpeg

  # Pip dependencies
  - pip
  - pip:
    # PyTorch with CUDA (adjust for target cuda version as needed)
    - torch
    - torchaudio

    # Result data noun (TranscriptionResult). Substrate (cjm-plugin-system) is
    # AUTO-INJECTED by `cjm-ctl install-all` (schema v2), so it is no longer
    # listed here — same reason it was dropped from plugins.yaml interface_libs.
    - cjm-capability-primitives

    # Shared torch helpers for plugins (release_model / cuda_oom / resolve_device)
    - cjm-torch-plugin-utils

    # Whisper and dependencies
    - openai-whisper
    - soundfile
    - numpy

    # Utilities
    - fastcore
    - pandas

    # Worker dependencies (FastAPI, uvicorn, psutil, httpx)
    - fastapi
    - uvicorn
    - psutil
    - httpx
